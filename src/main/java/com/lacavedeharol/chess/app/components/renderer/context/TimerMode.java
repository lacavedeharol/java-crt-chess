package com.lacavedeharol.chess.app.components.renderer.context;

/**
 * The timer mode.
 */
public enum TimerMode {
    /** Blitz timer mode. */
    BLITZ,
    /** Rapid timer mode. */
    RAPID,
    /** Classic timer mode. */
    CLASSIC
}
